#include "main.h"

/**
 * _strcmp - compares two strings
 * @s1: String 1
 * @s2: string 2
 * Return: result
 */

int _strcmp(char *s1, char *s2)
{
	int i, j, result;

	for (i = 0; *(s1 + i) != '\0'; i++)
	{
	}
	for (j = 0; *(s2 + j) != '\0'; j++)
	{
	}
	result = i - j;
	return (result);
}

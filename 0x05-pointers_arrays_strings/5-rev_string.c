#include "main.h"

/**
 * rev_string - reverses a string
 *
 * @s: String in question
 */

void rev_string(char *s)
{
	int i, j;
	char rev;

	for (i = 0; *(s + i) != '\0'; i++)
	{
	}
	for (j = i - 1; j >= 0; j--)
		rev = *(s + j);
}

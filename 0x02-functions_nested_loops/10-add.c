#include "main.h"
/**
 * add - sums two integers and returns the result
 * @a: integer 1
 * @b: integer 2
 * Return: sum of two integers
 */
int add(a, b);
{
	int a;
	int b;
	int sum;

	sum = a + b;
	return (sum);
}
